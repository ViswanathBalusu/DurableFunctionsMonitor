# The "overrides" block in package.json resolves react/react-dom for react-vis through
# "$@mui/material". Modern npm rejects that reference unless legacy peer-dep resolution is on,
# so every `npm install` here - CLI, Visual Studio and CI alike - needs this.
legacy-peer-deps=true
