<script lang="ts">
  // The status spine is a plain `<td class="spine">` whose colour comes from the row's `data-st`
  // attribute (dfm-ui.css L157). DataTable renders it directly; this component exists so a table
  // that builds its own rows can still get it right.
</script>

<td class="spine"></td>
